{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ticket list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Removedat</th>
                <th>Barcode</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Paidat</th>
                <th>Paidprice</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tickets_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.removedAt %}{{ entity.removedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.barcode }}</td>
                <td>{{ entity.firstname }}</td>
                <td>{{ entity.lastname }}</td>
                <td>{% if entity.paidAt %}{{ entity.paidAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.paidPrice }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tickets_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tickets_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tickets_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
