{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserGroup</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Updatedat</th>
                <td>{{ entity.updatedAt|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Removedat</th>
                <td>{{ entity.removedAt|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Ishidden</th>
                <td>{{ entity.isHidden }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Informations</th>
                <td>{{ entity.informations }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('usergroups') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('usergroups_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
