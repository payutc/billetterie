{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserGroup list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Removedat</th>
                <th>Ishidden</th>
                <th>Title</th>
                <th>Informations</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('usergroups_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.removedAt %}{{ entity.removedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.isHidden }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.informations }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('usergroups_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('usergroups_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('usergroups_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
